{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prestart": "pushd api && npm run build && popd",
    "start": "swa start"
  },
  "devDependencies": {
    "@types/deep-equal": "^1.0.1",
    "react-scripts": "^5.0.1"
  }
}
